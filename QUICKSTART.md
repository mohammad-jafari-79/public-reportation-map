# راهنمای سریع شروع کار 🚀

## مرحله ۱: نصب PostgreSQL

### Windows:

1. دانلود از https://www.postgresql.org/download/windows/
2. نصب PostgreSQL و pgAdmin
3. در pgAdmin یک دیتابیس جدید بسازید با نام `city_reports`
4. PostGIS Extension را نصب کنید:
   - راست کلیک روی دیتابیس → Extensions → Create Extension → postgis

## مرحله ۲: راه‌اندازی Backend

```powershell
# رفتن به پوشه backend
cd backend

# ایجاد محیط مجازی Python
python -m venv venv

# فعال کردن محیط مجازی
.\venv\Scripts\activate

# نصب وابستگی‌ها
pip install -r requirements.txt

# کپی کردن فایل .env
copy .env.example .env

# ویرایش فایل .env و تنظیم اطلاعات دیتابیس:
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/city_reports
# SECRET_KEY=your-very-secret-key-here-change-this

# اولیه‌سازی دیتابیس (ایجاد جداول و داده‌های اولیه)
python init_db.py

# اجرای سرور
python main.py
```

سرور Backend روی http://localhost:8000 اجرا می‌شود
مستندات API: http://localhost:8000/docs

## مرحله ۳: راه‌اندازی Frontend

ترمینال جدید باز کنید:

```powershell
# رفتن به پوشه frontend
cd frontend

# نصب وابستگی‌ها
npm install

# اجرای سرور توسعه
npm run dev
```

Frontend روی http://localhost:3000 اجرا می‌شود

## مرحله ۴: ورود به سیستم

1. مرورگر را باز کنید: http://localhost:3000
2. روی "ورود" کلیک کنید
3. اطلاعات ادمین پیش‌فرض:
   - ایمیل: `admin@cityreports.com`
   - رمز: `admin123`

## تست سریع ✅

1. **ثبت کاربر جدید**:

   - روی "ثبت‌نام" کلیک کنید
   - یک حساب کاربری بسازید

2. **ثبت گزارش**:

   - وارد شوید
   - روی "+ ثبت گزارش جدید" کلیک کنید
   - فرم را پر کنید
   - عکس اضافه کنید (اختیاری)

3. **مشاهده گزارش روی نقشه**:

   - روی مارکر روی نقشه کلیک کنید
   - جزئیات گزارش نمایش داده می‌شود

4. **رای دادن**:

   - روی گزارش کلیک کنید
   - دکمه 👍 را بزنید

5. **پنل ادمین**:
   - با حساب ادمین وارد شوید
   - "پنل ادمین" را باز کنید
   - گزارشات را تایید/رد کنید

## مشکلات رایج 🔧

### Backend اجرا نمیشه:

- مطمئن شوید PostgreSQL در حال اجراست
- پسورد دیتابیس در `.env` صحیح است
- پورت 8000 آزاد است

### Frontend اجرا نمیشه:

- `npm install` را دوباره اجرا کنید
- پورت 3000 آزاد است
- Node.js نسخه 18+ نصب است

### عکس‌ها آپلود نمیشن:

- پوشه `backend/uploads` ایجاد شده
- دسترسی نوشتن روی پوشه وجود دارد

### نقشه نمایش داده نمیشه:

- اینترنت متصل است (برای OpenStreetMap)
- Console مرورگر را چک کنید

## دستورات مفید 📝

### Backend:

```powershell
# اجرای سرور با reload خودکار
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Reset دیتابیس
python init_db.py

# ایجاد کاربر ادمین جدید (در Python shell)
python
>>> from app.database import SessionLocal
>>> from app.models import User
>>> from app.auth import get_password_hash
>>> db = SessionLocal()
>>> admin = User(name="Admin2", email="admin2@test.com", password_hash=get_password_hash("pass123"), role="admin")
>>> db.add(admin)
>>> db.commit()
>>> exit()
```

### Frontend:

```powershell
# Build برای production
npm run build

# Preview build
npm run preview

# Clear cache
rm -rf .nuxt node_modules
npm install
```

## نکات امنیتی 🔒

⚠️ قبل از استفاده واقعی:

1. `SECRET_KEY` را در `.env` تغییر دهید
2. رمز ادمین پیش‌فرض را تغییر دهید
3. `allow_origins` در `main.py` را محدود کنید
4. HTTPS استفاده کنید
5. Firewall تنظیم کنید

## منابع بیشتر 📚

- [FastAPI Docs](https://fastapi.tiangolo.com/)
- [Nuxt 3 Docs](https://nuxt.com/)
- [Leaflet Docs](https://leafletjs.com/)
- [PostgreSQL Docs](https://www.postgresql.org/docs/)

موفق باشید! 🎉
